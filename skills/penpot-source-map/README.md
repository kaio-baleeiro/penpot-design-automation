# penpot-source-map

Captura e registra URL, screenshot, runtime e repositório de código. Mede
viewport/documento/frame, inventaria assets exatos e escreve os artefatos de
origem antes da construção.

Use os wrappers em `scripts/` e consulte `references/frame-sizing.md` quando o
frame não for limitado ao viewport.
