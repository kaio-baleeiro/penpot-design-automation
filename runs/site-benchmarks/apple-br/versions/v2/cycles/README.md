# Ciclos Apple BR v2

Diretório de evidência append-only do avaliador. Cada `cycle-N/` contém score,
issues, relatório e imagens locais; as cópias PNG públicas ficam em
`../../../analysis/` com nomes `v2-cycle-N-*`.
