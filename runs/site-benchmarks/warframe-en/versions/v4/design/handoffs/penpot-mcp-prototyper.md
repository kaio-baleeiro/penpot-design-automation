# Handoff — penpot-mcp-prototyper

Execute the serial MCP build in a fresh page named `Benchmark — Warframe EN v4`
and frame named `Warframe EN — Home — 1440x5837 v4`. The frame must be 1440×5837.
Use exact URLs from `../source/assets-manifest.json` and the complete bounded-v2
inventory. Create substantive descendants for all nine semantic section boards,
real tokens under `Warframe EN v4 Tokens`, and reusable components with at least
one instance in the visible frame. Append every MCP request/result to
`../penpot-mcp-log.jsonl`. Export only to `../exports/cycle-1.png`.

Do not score, validate, populate `cycles/`, or alter prior Warframe pages.
