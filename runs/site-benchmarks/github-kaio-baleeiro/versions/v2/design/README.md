# Design handoff

plan.json maps the source screen to the editable Penpot frame.
structure-inventory.json records the frame tree and is consumed by the
structural gate. penpot-mcp-log.jsonl records the MCP export.
