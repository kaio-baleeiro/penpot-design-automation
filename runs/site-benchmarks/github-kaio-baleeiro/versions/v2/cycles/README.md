# Validation cycles

Each cycle-N directory is immutable output from one official validation pass.
The pre-score Penpot export is kept under design/exports until the validator
creates the cycle directory.
The reviewer must authorize the next cycle after inspecting the issue handoff.
