# Rebuild v2 — GitHub `kaio-baleeiro`

Sanitized, versioned handoff for the next editable Penpot reconstruction. It is derived from `source/raw/full-page/source.png`, DOM/style evidence and immutable `cycle-2`; it does not replace canonical source files.

Source: `https://github.com/kaio-baleeiro` · viewport `1440x900` · frame `1440x1807` (`finite_document`) · horizontal policy `container_overflow`.

`cycle-2` scored 93.96 with 88.14% coverage but was rejected for regressing from cycle 1 and using a fictitious composition. Rebuild the actual profile: global header y=0–96; profile shell y=96–1692; footer y=1692–1807; 320px sidebar with 296px avatar; README card y=169; popular repositories y=660; contribution graph y=1077; contribution activity y=1313. Use the six real repository names and README copy in `section-map.json`. Do not use the screenshot as visible Penpot content.

No cookies, tokens or authenticated payloads are recorded here.
