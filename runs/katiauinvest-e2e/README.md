# Fixture KatiauInvest

Este run é um benchmark sanitizado usado como cobaia do workflow. Ele preserva
manifestos, frame specs, scores e decisões sem incluir capturas privadas.

O resultado histórico encerra em `NEEDS_REVIEW` porque duas telas não passaram
individualmente no gate de 90%; a média nunca substitui a aprovação por tela.
