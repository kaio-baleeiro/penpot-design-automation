# Scripts da composição Penpot

Scripts para `up`, `down`, `healthcheck`, `validate`, `logs`, `backup`,
`restore` e `update`. Eles operam os volumes externos preservados da migração;
`down` não remove volumes e `restore` exige confirmação explícita.
