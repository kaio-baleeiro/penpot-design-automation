# Gemini CLI entrypoint

@./AGENTS.md

Use `skills/penpot-design/SKILL.md` as the workflow entrypoint. Project
subagents are available in `.gemini/agents/`; their prompts point to the
canonical profiles in `agents/profiles/`.
